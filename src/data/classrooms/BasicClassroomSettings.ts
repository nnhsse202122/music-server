type BasicClassroomSettings = {
    allow_submissions: boolean,
    submissions_require_tokens: boolean,
    joinable: boolean,
    playlist_visible: boolean
};

export default BasicClassroomSettings;